#include <stdio.h>
 
int main()
{
    
    // Calculate and Print
    // the size of integer type
    printf("Size of int is: %ld Bytes", sizeof(int));
 
    // Calculate and Print
    // the size of charType
    printf("\nSize of char is: %ld Bytes", sizeof(char));
 
    // Calculate and Print
    // the size of floatType
    printf("\nSize of float is: %ld Bytes", sizeof(float));
 
    // Calculate and Print
    // the size of doubleType
    printf("\nSize of double is: %ld Bytes", sizeof(double));
 
    return 0;
}